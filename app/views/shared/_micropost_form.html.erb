<%= form_for(@micropost) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="inscont">
	<%= f.text_area :content, placeholder: "Inserisci un contenuto..." %>
	<div class="button">
		<%= f.submit "Pubblica", class: "btnLogin" %>
	</div>
  </div>
<% end %>